# Known-failing tests and collection errors — the CI ratchet allowlist.
#
# Each line is a vitest FAIL identifier, matched verbatim. A failure NOT listed
# here fails CI; a listed one is tolerated.
#
# Why identifiers and not a failure COUNT: a count lets a PR swap one failure
# for another and stay green, and it cannot work on a partial run — the PR job
# only runs tests affected by the diff, so its failure count is meaningless
# against a whole-suite baseline. Identifiers work for both.
#
# DELETE lines as you fix them; the ratchet prints entries that have started
# passing so this file can be pruned. Never ADD a line to turn a red build
# green — that is the habit this gate exists to break.
#
# Captured 2026-07-28 at master 62ffc71e8: the first run in this repo's history
# where the suite actually completed (~40s, 630 files, 7200 tests). Three
# consecutive runs produced byte-identical failure sets.
#   56 failing tests across 8 files, + 10 files that fail to COLLECT (0 tests) —
#   missing TEST_DATABASE_URL, a vi.mock hoisting error, `jest is not defined`.

# --- Intermittent. Listed so a flake cannot redden a build; expect this in the
# --- "started passing" report most runs. Worth fixing properly — see #2279.
# ---   module-structure: fails roughly 1 run in 6, no clear trigger.
src/modules/noc/__tests__/module-structure.test.ts > Maintenance Module Structure > Server Exports (index.ts) > should export from index.ts without errors
