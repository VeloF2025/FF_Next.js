{
  "//": "The root tsconfig excludes scripts/**, so `tsc --noEmit` never sees this engine. Without this file the bench is untyped in practice. Run: npx tsc --noEmit -p scripts/vlm-bench",
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["**/*.ts"],
  "exclude": []
}
