---
description: FibreFlow Next.js — project overview, stack, directory structure
alwaysApply: true
---

<!-- Auto-generated by /portability from CLAUDE.md. Do not edit manually. -->

# FibreFlow Next.js

Fiber network project management application for Velocity Fibre.

- Framework: Next.js 14+ (Pages Router primary, App Router for new routes)
- Auth: PostgreSQL-based RBAC (no Clerk, no Auth0)
- Database: Self-hosted Supabase Postgres on Velocity (`100.96.203.105:5437`) — single DB shared by dev + production
- Storage: VF Storage on `100.96.203.105:8091`, served via `app.fibreflow.app/storage/`
- Staff portal: `/my` (dark theme, PWA) for time/attendance, payslips, receipts, fleet

## Project structure

```
src/{modules,components,services,lib}/
.claude/{modules,skills,knowledge-base}/
pages/api/                    # API routes (flattened, not [id]/sub.ts)
```

## Path-scoped rules — two parallel doc systems

| Location | Role | Loading |
|----------|------|---------|
| `src/modules/<name>/.claude.md` (~56 files) | Quick reference / module gotchas (≤50 lines) | Auto-loaded when working inside that module |
| `.claude/modules/<name>.md` (~59 files) | Full reference / deep-dive docs | Explicit-load (read when needed) |

Keep auto-loaded files short; push detail into `.claude/modules/<name>.md`. A drift gate (`npm run claude-md:check`) verifies every backtick-quoted `src/...` path resolves.

## Tech debt

`lib/db/pool.js` still imports `@neondatabase/serverless` (webpack-aliased via `src/lib/neon-shim.ts`). New code uses `pg.Pool` via `@/lib/db` or `@/lib/db-pool`. If a route 500s, check for the Neon serverless import first.

**Two Drop Tables — DO NOT CONFUSE**:
- `drops` — SOW imports (`/api/sow/drops`)
- `qa_photo_reviews` — WhatsApp QA data (`/api/wa-monitor-*`)
