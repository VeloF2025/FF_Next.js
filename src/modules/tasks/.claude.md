# Module: tasks
<!-- Task management dashboard UI (navigation shell; data layer is minimal) -->

## Purpose
Task management navigation hub for creating, assigning, and tracking work items across projects and teams.

## Key Files
| File | Purpose |
|------|---------|
| `TasksDashboard.tsx` | Navigation card grid + tab bar (All / My / Team / Completed) |

## API Endpoints
| Method | Path | Purpose |
|--------|------|---------|
| GET | `/api/field/tasks` | List field tasks |
| GET/PUT | `/api/field/tasks/[id]` | Task detail / update |

## Critical Rules
- `TasksDashboard` is currently a navigation shell with **hardcoded placeholder counts** — not wired to live data
- The task sub-pages (`/app/tasks/new`, `/app/tasks/in-progress`, etc.) are separate routes, not rendered inside this module
- Tasks link to parent project or meeting via FK; always populate `project_id` when creating

## Common Issues
| Issue | Fix |
|-------|-----|
| Counts show stale numbers (48/12/36) | Dashboard is placeholder — counts are hardcoded, not from DB |

<!-- Auto-updated by /kb. Last: 2026-05-12 -->
