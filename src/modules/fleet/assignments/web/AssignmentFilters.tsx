import type { AssignmentSource } from '../types';
export interface AssignmentFilterValue { from: string; to: string; projectId: string; siteId: string; source: AssignmentSource | '' }
export function AssignmentFilters({ value, onChange }: { value: AssignmentFilterValue; onChange: (value: AssignmentFilterValue) => void }) {
  return <div className="flex flex-wrap gap-3">{(['from', 'to'] as const).map((key) => <input aria-label={key} key={key} type="date" value={value[key]} onChange={(e) => onChange({ ...value, [key]: e.target.value })} />)}<input aria-label="Project" placeholder="Project ID" value={value.projectId} onChange={(e) => onChange({ ...value, projectId: e.target.value })} /><input aria-label="Site" placeholder="Site ID" value={value.siteId} onChange={(e) => onChange({ ...value, siteId: e.target.value })} /><select aria-label="Source" value={value.source} onChange={(e) => onChange({ ...value, source: e.target.value as AssignmentSource | '' })}><option value="">All sources</option><option value="daily_override">Daily override</option><option value="roster">Roster</option><option value="vehicle_project">Vehicle project</option><option value="home_site">Home site</option><option value="unassigned">Unassigned</option></select></div>;
}
