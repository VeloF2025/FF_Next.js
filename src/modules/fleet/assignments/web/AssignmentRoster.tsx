import type { AssignmentRosterEntry } from '../types';
export function AssignmentRoster({ rows, selected, onSelect }: { rows: AssignmentRosterEntry[]; selected: string[]; onSelect: (ids: string[]) => void }) {
  return <div className="overflow-x-auto"><table><thead><tr><th>Select</th><th>Staff</th><th>Source</th><th>Dates</th><th>Vehicle</th></tr></thead><tbody>{rows.map((row) => { const key = row.assignmentId ?? `${row.staffId}-${row.source}`; return <tr key={key}><td><input aria-label={`Select ${row.staffId}`} type="checkbox" checked={selected.includes(key)} onChange={(e) => onSelect(e.target.checked ? [...selected, key] : selected.filter((id) => id !== key))} /></td><td>{row.staffId}</td><td>{row.source}</td><td>{row.startDate ?? '—'} – {row.endDate ?? '—'}</td><td>{row.vehicleAssignmentId ?? 'No vehicle'}</td></tr>; })}</tbody></table></div>;
}
