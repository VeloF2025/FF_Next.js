# Module: daily-progress
<!-- Dashboard for entering and viewing daily field KPIs -->

## Purpose
Legacy, unmounted dashboard shell for daily progress KPI entry and reporting. The live route is `/projects/progress`; both `/daily-progress` and `/projects/daily-progress` permanently redirect there.

## Key Files
| File | Purpose |
|------|---------|
| `DailyProgressDashboard.tsx` | Entry point — 6 nav cards + KPI summary strip (all values static/zero; routes to sub-pages) |

## Live Routes (Pages Router)
| Route | Purpose |
|-------|---------|
| `/projects/progress` | Canonical project progress page |
| `/daily-progress` | Permanent redirect to the canonical page |
| `/projects/daily-progress` | Permanent redirect to the canonical page |

## Critical Rules
- `DailyProgressDashboard.tsx` is not imported by a live page; its KPI cards are hardcoded zeros and its six `/app/daily-progress/*` links do not resolve
- Do not add the missing shell sub-pages by default — extend the live `/projects/progress` flow unless the legacy dashboard is explicitly being revived
- This module has a single top-level component; no services/hooks/types yet

## Common Issues
| Problem | Fix |
|---------|-----|
| KPIs always show 0 | The legacy dashboard is an unmounted shell with hardcoded values |
| Shell card opens a 404 | The six target routes were never implemented; use `/projects/progress` |

<!-- Auto-updated by /kb. Last: 2026-05-12 -->
