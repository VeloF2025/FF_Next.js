# Module: daily-progress
<!-- Dashboard for entering and viewing daily field KPIs -->

## Purpose
Navigation hub for daily progress KPI entry and reporting (poles installed, fiber pulled, homes connected, quality scores).

## Key Files
| File | Purpose |
|------|---------|
| `DailyProgressDashboard.tsx` | Entry point — 6 nav cards + KPI summary strip (all values static/zero; routes to sub-pages) |

## Sub-pages (Pages Router)
| Route | Purpose |
|-------|---------|
| `/app/daily-progress/entry` | Submit today's KPI data |
| `/app/daily-progress/view` | Track daily progress trends |
| `/app/daily-progress/weekly` | Weekly performance overview |
| `/app/daily-progress/monthly` | Monthly analysis |
| `/app/daily-progress/teams` | Compare team metrics |
| `/app/daily-progress/export` | Download progress reports |

## Critical Rules
- `DailyProgressDashboard.tsx` is a shell — KPI summary cards currently show hardcoded zeros; actual data lives in sub-pages
- This module has a single top-level component; no services/hooks/types yet — new code needs these directories created first
- Sub-page routes under `/app/daily-progress/` must be flat (not nested dynamic) per project convention

## Common Issues
| Problem | Fix |
|---------|-----|
| KPIs always show 0 | Dashboard is a nav shell; data is in sub-pages not yet fully implemented |
| Missing sub-page | Check `pages/app/daily-progress/` for page files — some may be stubs |

<!-- Auto-updated by /kb. Last: 2026-05-12 -->
