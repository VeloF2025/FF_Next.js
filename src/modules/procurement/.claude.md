# Module: procurement
<!-- 60 lines max - BOQ, RFQ, PO, Stock management -->

## Purpose
Procurement workflow management: Bill of Quantities (BOQ), Request for Quotes (RFQ), Purchase Orders (PO), and stock tracking.

## Navigation Structure (Jan 2026)
Simplified 6-item flat menu with tab-based pages:

| Menu | Route | Tabs |
|------|-------|------|
| Dashboard | `/procurement` | Overview |
| Sourcing | `/procurement/sourcing` | Suppliers, BOQ, RFQ |
| Purchasing | `/procurement/purchasing` | Quotes, Requisitions, PO, GRN |
| Inventory | `/procurement/inventory` | Stock, Items, Categories, Bundles, Takes, Field |
| Financial | `/procurement/financial` | Budget, Cost Centers |
| Approvals | `/procurement/approvals` | Workflow |

**Tab Persistence:** `useTabPersistence` hook syncs URL params + localStorage.

## Critical Rules
- NEVER modify prices without HITL confirmation
- ALWAYS validate stock levels before creating PO
- ALWAYS link RFQ items to BOQ line items

## Key Files
| File | Purpose |
|------|---------|
| `hooks/useTabPersistence.ts` | Tab state with URL + localStorage sync |
| `services/boq/boqCrud.ts` | BOQ CRUD operations |
| `services/rfq/rfqCrud.ts` | RFQ CRUD operations |
| `components/boq/BOQList.tsx` | BOQ list view |
| `components/rfq/RFQList.tsx` | RFQ list view |

## API Endpoints
| Method | Endpoint | Purpose |
|--------|----------|---------|
| GET | `/api/procurement/boq` | List BOQs |
| POST | `/api/procurement/boq` | Create BOQ |
| GET | `/api/procurement/rfq` | List RFQs |
| POST | `/api/procurement/rfq/{id}/convert-to-po` | Convert RFQ to PO |

## Database
- `boq` - Bill of Quantities
- `boq_items` - BOQ line items
- `rfq` - Request for Quotes
- `rfq_items` - RFQ line items
- `purchase_orders` - Purchase orders
- `stock_items` - Inventory

## Workflow
```
BOQ → RFQ → Quotes → PO → GRN → Stock
```

## Common Issues
| Issue | Fix |
|-------|-----|
| RFQ not loading items | Check BOQ link, verify `boq_id` FK |
| Stock mismatch | Run stock reconciliation |
| UUID type errors | Cast varchar `project_id` to UUID in JOINs |
| LIMIT/OFFSET errors | Use numbers, not strings, for pagination params |
| Numeric string values | Convert with `Number()` before arithmetic |

## Learnings (Consolidated 2026-01-21)

### Neon PostgreSQL Type Handling
- Neon sometimes returns numeric values as strings
- Always convert with `Number()` before arithmetic operations
- Always cast types explicitly in JOINs with UUID columns: `project_id::UUID`

### Column Name Fixes
- Use `boq_item_id` not `boq_id` in boq_items table
- Use correct column names in ORDER BY (e.g., `name` not `cost_center_name`)

---
<!-- Auto-updated by /kb. Last: 2026-01-22 - Updated navigation structure -->
