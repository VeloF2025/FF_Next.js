# Module: attendance
<!-- Keep under 50 lines - reference, don't duplicate -->

## Purpose
Staff time-and-attendance: geofenced clock-in/out (selfie), corrections workflow, weekly lock, bulk export, Cartrack GPS cross-verification, /my portal auth.

## Critical Rules
- NEVER project DATE columns without `to_char(col, 'YYYY-MM-DD')` — pg returns JS Date at server-local midnight which JSON-shifts in SAST
- ALWAYS gate correction approvals through `attendance_adjustments` status machine (`pending → approved/rejected`) — never update `attendance_entries` directly from a staff-facing route
- NEVER remove geofence checks in `portal/geofenceUtils.ts` — geofence is the legal record of presence

## Key Files
| File | Purpose |
|------|---------|
| `corrections/queries.ts` | `attendance_adjustments` CRUD + supervisor joins |
| `corrections/lockQueries.ts` | Weekly lock read/write |
| `portal/authMiddleware.ts` | /my session validation (separate from main FF auth) |
| `portal/geofenceUtils.ts` | Haversine geofence check against `site_geofences` |
| `portal/clockUtils.ts` | Clock entry creation + overlap detection |
| `alerts/cartrackWaAlert.ts` | Cartrack discrepancy WA alert |

## API Endpoints (subset)
| Method | Endpoint | Purpose |
|--------|----------|---------|
| POST | `/api/my/attendance/clock-in` `/clock-out` | Staff clock |
| GET | `/api/my/attendance/current` `/history` | Self state + history |
| GET, POST | `/api/my/attendance-corrections` | Correction request |
| POST | `/api/staff/attendance-corrections-review` | Approve/reject corrections |
| POST | `/api/staff/attendance-bulk-lock` | Bulk weekly lock |
| GET | `/api/staff/attendance-search` `/-export` `/-report` | HR Pulse search + outputs |

## Database Tables
- `attendance_entries`, `attendance_adjustments`, `attendance_weekly_locks`
- `attendance_daily_summaries`, `attendance_credentials`, `attendance_auth_sessions`
- `attendance_exceptions`, `attendance_gps_verifications`, `attendance_bulk_action_audit`, `public_holidays`

## Dependencies
- **External:** Cartrack API, WhatsApp bridge
- **Internal:** `@/lib/db-pool`, `portal/geofenceUtils.ts`, `alerts/cartrackWaAlert.ts`

## Related Skills
<!-- No dedicated skill yet -->

---
<!-- Auto-generated by /kb command. Last updated: 2026-04-29 -->
