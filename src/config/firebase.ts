/**
 * Firebase config stub — legacy pole-tracker/project services use firebase/firestore.
 * Firebase is not installed; this stub satisfies the import resolution.
 * These services are superseded by the Neon PostgreSQL equivalents.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const db: any = null;
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const app: any = null;
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const auth: any = null;
