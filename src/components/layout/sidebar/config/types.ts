/**
 * Navigation configuration types
 * Re-exports from parent types.ts for backwards compatibility
 */

export type { NavItem, NavSection, SidebarStyles } from '../types';
export { type Permission } from '@/types/auth.types';
