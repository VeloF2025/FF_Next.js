---
trigger: always_on
description: Git workflow and deployment rules for FibreFlow Next.js
---

<!-- Auto-generated by /portability from CLAUDE.md. Do not edit manually. -->

# Git workflow (mandatory)

- All changes go through Pull Requests — never commit directly to master.
- Never edit files in deploy directories (`/home/velo/fibreflow-dev/`, `/home/velo/fibreflow-production/`).
- Branch naming: `feature/<name>`, `fix/<name>`, `refactor/<name>`.
- Run `npm run ci:quick` before opening any PR.
- Use `gh` CLI for GitHub operations.

# Destructive commands require confirmation

Force-push, `git reset --hard`, branch deletion, `rm -rf`, merges into master, schema migrations, process kills (no `pkill -f node`/`pkill -f npm`). Ask when unsure.

# Deployment

| Environment | URL | Port |
|-------------|-----|------|
| Dev | dev.fibreflow.app | 3005 |
| Production | app.fibreflow.app | 3000 |
| Local | localhost:3004 | 3004 |

| Window | Dev | Production |
|--------|-----|------------|
| Business hours (08:00–17:00 SAST, Mon–Fri) | Allowed | **BLOCKED** |
| After hours + weekends | Allowed | Promote from dev |
| Emergency | Allowed | `--force` required |

**Hein's explicit approval (the project owner) is required for ALL production deployments** — personal sign-off, not a generic CI gate.

Always use `bash scripts/deploy-local.sh dev|production`. Never manual `git pull + build + restart` (causes 500s).
